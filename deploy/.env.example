# Port for the app on your PC
APP_PORT=8080

# --- Without Docker: free cloud PostgreSQL (pick one) ---
# 1) Create a free project at https://neon.tech
# 2) Copy the connection string and paste it here:
# DATABASE_URL=postgres://user:password@ep-xxxx.region.aws.neon.tech/neondb?sslmode=require

# --- With Docker only (local PostgreSQL in container) ---
POSTGRES_USER=litematic
POSTGRES_PASSWORD=litematic
POSTGRES_DB=litematic
